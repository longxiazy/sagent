# 环境变量（最重要，保护 API Key）
.env
.env.local
.env.*.local

# 依赖目录
node_modules/
client/node_modules/

# Vite 构建产物
client/dist/
client/.vite/
agent/tools/macos/helper/bin/

# 系统文件
.DS_Store
Thumbs.db

# 编辑器
.idea/
.vscode/
*.suo
*.swp

# 日志
logs/
*.log
npm-debug.log*

# 运行时数据（agent memory / checkpoints / logs / traces 等）
# 必须用前导 / 锚定到项目根，否则会匹配所有层级同名目录
# （早先写成 `data` 时把 client/src/data/ 也错杀过）
/data/
/data-docker/

/artifacts/

todo.md

.claude/
